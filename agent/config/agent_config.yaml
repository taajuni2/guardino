watch_directories:
  - /home
  - /var/www

blacklist_paths:
  - /proc
  - /sys
  - /dev
  - /tmp

kafka:
  broker: "host.docker.internal:9092"
  topic: "agent-events"

log_level: "INFO"


excluded_extensions:
  - tmp
  - log

included_extensions:
  - exe
  - docx
  - pdf